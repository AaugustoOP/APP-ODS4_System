
  # Pomodoro Focus

  Aplicação Pomodoro voltada para o foco, produtividade e apoio ao ODS 4.

  Disponível em https://aaugustoop.github.io/APP-ODS4_System/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.